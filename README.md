# Lab-1-Group-Word-Search

This is currently on the base which reads the file and places it's content into a 2d array. Make sure you put the file into the same directory (folder) so your IDE will use it properly. Attached is the grid of characters I have prepared for testing.
